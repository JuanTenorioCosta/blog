---
const { icon, link, text, target } = Astro.props
---
<a href={link} target={ target ? target : "_self" }>
  {icon ? <span class="icon"></span> : <></>}
  {text ? <span class="text">{text}</span> : <></>}
</a>
